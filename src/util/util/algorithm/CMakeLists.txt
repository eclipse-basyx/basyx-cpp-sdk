
SET(SOURCE_FILES_ALGORITHM
)

SET(HEADER_FILES_ALGORITHM
   ${CMAKE_CURRENT_SOURCE_DIR}/string.hpp
)

###
target_sources(${BASYX_UTIL_LIB_SUFFIX}
   PRIVATE
   ${SOURCE_FILES_ALGORITHM}
)

target_sources(${BASYX_UTIL_LIB_SUFFIX}
   PRIVATE
   ${HEADER_FILES_ALGORITHM}
)
