
SET(SOURCE_FILES_CONTAINER
)

SET(HEADER_FILES_CONTAINER
   ${CMAKE_CURRENT_SOURCE_DIR}/container.hpp
)

###
target_sources(${BASYX_UTIL_LIB_SUFFIX}
   PRIVATE
   ${SOURCE_FILES_CONTAINER}
)

target_sources(${BASYX_UTIL_LIB_SUFFIX}
   PRIVATE
   ${HEADER_FILES_CONTAINER}
)
