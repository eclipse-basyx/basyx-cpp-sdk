
SET(SOURCE_FILES_OPTIONAL
)

SET(HEADER_FILES_OPTIONAL
   ${CMAKE_CURRENT_SOURCE_DIR}/optional.hpp
)

###
target_sources(${BASYX_UTIL_LIB_SUFFIX}
   PRIVATE
   ${SOURCE_FILES_OPTIONAL}
)

target_sources(${BASYX_UTIL_LIB_SUFFIX}
   PRIVATE
   ${HEADER_FILES_OPTIONAL}
)
